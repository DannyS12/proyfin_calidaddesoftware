<!-- Encabezado tipo Dashboard -->
<div class="d-flex align-items-center justify-content-between mb-4">
  <div>
    <h1 class="h3 mb-1">Editar Categoría</h1>
    <p class="text-muted mb-0">Modifica los datos de la categoría seleccionada</p>
  </div>
  <div>
    <a href="/verCatProd" class="btn btn-outline-secondary">
      <i class="bi bi-arrow-left"></i> Volver a la lista
    </a>
  </div>
</div>

<!-- Formulario -->
<div class="card shadow-sm">
  <div class="card-body">
    <form id="categoriaForm" action="/updateCatProd" method="POST" novalidate>
      <input type="hidden" name="Id" value="<%= categoria.IdCategoria %>">

      <div class="mb-3">
        <label for="Nombre" class="form-label">Nombre</label>
        <input
          type="text"
          class="form-control"
          id="Nombre"
          name="Nombre"
          value="<%= categoria.Nombre %>"
          required
          minlength="3"
          maxlength="50"
          pattern="[A-Za-zÁÉÍÓÚáéíóúÑñ\s]+"
          placeholder="Ej. Bebidas"
        />
        <div class="invalid-feedback">
          Por favor ingresa un nombre válido (solo letras y espacios, mínimo 3 caracteres).
        </div>
      </div>

      <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">
          <i class="bi bi-save me-1"></i> Actualizar
        </button>
        <a href="/verCatProd" class="btn btn-secondary">Cancelar</a>
      </div>
    </form>
  </div>
</div>

<!-- Validación Bootstrap -->
<script>
  (function () {
    'use strict';
    const form = document.getElementById('categoriaForm');
    form.addEventListener('submit', function (event) {
      if (!form.checkValidity()) {
        event.preventDefault();
        event.stopPropagation();
      }
      form.classList.add('was-validated');
    }, false);
  })();
</script>